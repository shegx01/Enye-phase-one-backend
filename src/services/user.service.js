const httpStatus = require('http-status');
const ApiError = require('../utils/ApiError');

const queryUsers = async (filter, options) => {
  const users = await null
  return users;
};

module.exports = {
  queryUsers,

};
