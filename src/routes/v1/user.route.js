const express = require('express');
const userController = require('../../controllers/user.controller');

const router = express.Router();
/*
router
  .route('/rates')
  .get(null, validate(userValidation.getUsers), userController.getUsers);
*/

module.exports = router;

